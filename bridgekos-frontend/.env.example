# ------------------------------------------------------------------
# BridgeKos Frontend Environment
# Copy to .env and adjust as needed.
# ------------------------------------------------------------------

# Base URL of the BridgeKos backend REST API
VITE_API_URL=http://localhost:4000/api/v1

# Dev server proxy target (used by vite proxy for /api requests in dev)
VITE_PROXY_TARGET=http://localhost:4000